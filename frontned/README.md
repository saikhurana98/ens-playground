Frontned - React + TypeScript app (Vite)

How to run

- Install dependencies: yarn
- Dev server: yarn dev
- Build: yarn build

This project provides a minimal React + TypeScript boilerplate. The default page contains a bare search box that does nothing yet.
